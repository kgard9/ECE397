#include <Wire.h>
#include <Adafruit_MCP4725.h>
#include <SPI.h>
#include <nRF24L01.h>
#include <RF24.h>

#define MIC_PIN A0    // Mic input pin
#define CE_PIN 7
#define CSN_PIN 8

RF24 radio(CE_PIN, CSN_PIN);
Adafruit_MCP4725 dac;

// Two addresses for two-way communication
const byte address1[6] = "00001"; 
const byte address2[6] = "00002"; 

#define FILTER_SIZE 4
int filterBuffer[FILTER_SIZE] = {0};
int filterIndex = 0;

void setup() {
  Serial.begin(115200);

  if (!dac.begin(0x62)) {
    Serial.println("DAC not found!");
    while (1);
  }
  Serial.println("
